# brainfuck-interpreter
Minimal Brainfuck interpreter in Racket. Run the interpreter in a terminal using `./brainfuck.rkt instructions.bf`, where `instructions.bf` is the name of a file containing Brainfuck instructions (the file extension is irrelevant).
